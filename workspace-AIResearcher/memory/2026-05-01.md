## 周报模板美化（22:05）

用户反馈周报格式太丑，要求美化。

**改动内容：**
- 去掉大量 `━` 装饰线，统一用 `━━━━━━━━━━━━━━━━━━━━━` 做大区块分隔
- 企业动态从表格改为 `【企业名】（城市）` + 单行格式（飞书群聊不支持 markdown 表格渲染）
- 行动清单用 emoji 区分：🔴必访 / 🟡储备 / ⚠️预警
- 要闻格式统一为 `事件 | 影响星级 | 日期`
- 地市动态按城市分块，每块内用 `•` 列表

**更新文件：** `skills/ai-weekly-report/SKILL.md` 第七步模板
